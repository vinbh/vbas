---
name: CLAUDE.md is local-only
description: Do not commit or push CLAUDE.md — it stays in .gitignore as local developer documentation
type: feedback
originSessionId: 359993fe-1b21-460a-b941-fe860c086556
---
User keeps `CLAUDE.md` (the file generated by `/init`) as **local-only**, gitignored, never pushed to the public repo.

**Why:** Stated explicitly on 2026-05-02 during the M4.1+M4.2 push for the vbas project. Reasoning is implicit but consistent with vbas being a public source-available repo where the user wants public docs (`README.md`) to be the contributor-facing source of truth, and Claude-specific guidance to stay private to their working copy.

**How to apply:**
- After running `/init` in any of this user's projects, ensure `CLAUDE.md` is added to `.gitignore` before any commit. Same default applies project-wide unless user signals otherwise.
- Treat `CLAUDE.md` like `SESSION_NOTES.md` — both are personal scratch / Claude-facing context.
- Don't propose committing it as part of a "first commit" or "scaffold" sweep.
